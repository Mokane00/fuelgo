# Google Maps (get key from Google Cloud Console)
VITE_GOOGLE_MAPS_KEY=your_google_maps_api_key_here

# Stripe publishable key (optional — fetched from backend at runtime)
# VITE_STRIPE_PK=pk_live_...
